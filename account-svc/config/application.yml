spring:
  application:
    name: account-svc
  redis:
    host: 103.152.165.18
    port: 6397
    password: 123456a@
    lettuce:
      pool:
        max-active: 7
        max-idle: 7
        min-idle: 2
        max-wait: -1ms
      shutdown-timeout: 200ms
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://103.152.165.18:3306/nft_market?useSSL=false&serverTimezone=UTC
    username: hiepnh
    password: 123456aA@
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

server:
  port: ${ACCOUNT_SVC_PORT:32000}
  http2:
    enabled: true
  compression:
    enabled: true
    mime-types: application/json
    min-response-size: 1024

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:30000/eureka/}

logging:
  config: config/log4j2.xml

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false

zuul:
  host:
    socket-timeout-millis: 10000
    connect-timeout-millis: 10000

ribbon:
  ConnectTimeout: 10000
  ReadTimeout: 10000

application:
  version: ${VERSION:1.0}
  default_avatar: https://nhh-nft-market.s3.ap-southeast-1.amazonaws.com/76572de0-93fc-494a-920a-ca5c941277d3_1658661070720

